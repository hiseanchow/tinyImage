name: Release Tauri App

# 触发条件：当推送以 v 开头的标签（如 v1.0.0）时，自动开始打包
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: # 也支持在 GitHub 网页上手动点击运行

jobs:
  release:
    # 定义跨平台打包矩阵：同时启动一台 Mac 和一台 Windows 服务器
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - name: 获取项目代码 (Checkout)
        uses: actions/checkout@v4

      - name: 安装 pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: 安装 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: 安装 Rust 环境
        uses: dtolnay/rust-toolchain@stable

      - name: 安装前端依赖
        run: pnpm install

      - name: 编译并打包 Tauri 应用
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }} # 使用你推送的标签名作为版本号
          releaseName: 'TinyImage v${{ github.ref_name }}'
          releaseBody: '🚀 自动打包发布的最新版本，请在下方 Assets 下载对应的安装包。'
          releaseDraft: true # 开打包完成后先存为草稿，方便你检查后再公发布
          prerelease: false